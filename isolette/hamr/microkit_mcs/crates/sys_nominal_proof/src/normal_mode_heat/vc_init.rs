// Do not edit this file as it will be overwritten if HAMR codegen is rerun

//! Property normal_mode_heat -- Init-State VC: the initial state (all initialize + integration guarantees) satisfies the START assertion.
//! Each proof fn has an empty body: Verus discharges `requires ==> ensures`
//! via SMT; add proof hints in the body if a VC does not discharge.

#![allow(non_snake_case)]
#![allow(unused_variables)]
#![allow(unused_imports)]

use vstd::prelude::*;

use crate::assertions::*;
use crate::normal_mode_heat::assertions::*;
use crate::contracts::*;
use crate::system_state::SystemState;
use crate::write_frames::*;

verus! {

/** VC[0]: Init-State -- all initialize + integration guarantees |- START */
pub proof fn vc_init_state(st: SystemState)
  requires
    mri::initialize_RegulatorStatusIsInitiallyInit(st.regulator_status),
    mhs::initialize_initlastCmd(st.lastCmd),
    mhs::initialize_REQ_MHS_1(st.heat_control),
    mrm::initialize_REQ_MRM_1(st.regulator_mode),
    mmi::initialize_monitorStatusInitiallyInit(st.monitor_status),
    ma::initialize_REQ_MA_1(st.ma_lastCmd, st.alarm_control),
    mmm::initialize_REQ_MMM_1(st.monitor_mode),
    mmm::initialize_update_lastMonitorMode(st.lastMonitorMode, st.monitor_mode),
    oi::integration_spec_Allowed_LowerAlarmTempWstatus_guarantee(st.lower_alarm_tempWstatus),
    oi::integration_spec_Allowed_UpperAlarmTempWstatus_guarantee(st.upper_alarm_tempWstatus),
  ensures
    true /* START has no assertion */,
{}

} // verus!
