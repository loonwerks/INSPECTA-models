// Do not edit this file as it will be overwritten if HAMR codegen is rerun

//! Pre-Assert and Next-Assert VCs for every schedule transition.
//! Each proof fn has an empty body: Verus discharges `requires ==> ensures`
//! via SMT; add proof hints in the body if a VC does not discharge.

#![allow(non_snake_case)]
#![allow(unused_variables)]

use vstd::prelude::*;

use crate::assertions::*;
use crate::contracts::*;
use crate::system_state::SystemState;
use crate::write_frames::*;

verus! {

/** VC[1]: Pre-Assert -- pre_oi_1 |- OI compute + integration assumes */
pub proof fn vc_pre_assert_oi(st: SystemState)
  requires
    true /* pre_oi_1 has no assertion */,
  ensures
    true /* no assertions at out-places */,
{}

/** VC[2]: Next-Assert (task) -- pre_oi_1 + frames + OI postcondition |- END_OI_Assert */
pub proof fn vc_next_assert_task_oi(pre: SystemState, post: SystemState)
  requires
    true /* pre_oi_1 has no assertion */,
    oi_local_write_frame(pre, post),
    oi_global_write_frame(pre, post),
    oi::compute_spec_Allowed_AlarmTempWStatus_Ranges_guarantee(post.lower_alarm_tempWstatus, post.upper_alarm_tempWstatus),
    oi::compute_spec_Desired_Temps_Ordered_guarantee(post.lower_desired_tempWstatus, post.upper_desired_tempWstatus),
    oi::integration_spec_Allowed_LowerAlarmTempWstatus_guarantee(post.lower_alarm_tempWstatus),
    oi::integration_spec_Allowed_UpperAlarmTempWstatus_guarantee(post.upper_alarm_tempWstatus),
  ensures
    sys_assert_END_OI_Assert(post),
{}

/** VC[3]: Pre-Assert -- pre_ts_2 |- TS compute + integration assumes */
pub proof fn vc_pre_assert_ts(st: SystemState)
  requires
    true /* pre_ts_2 has no assertion */,
  ensures
    true /* no assertions at out-places */,
{}

/** VC[4]: Next-Assert (task) -- pre_ts_2 + frames + TS postcondition |- post_ts_3 */
pub proof fn vc_next_assert_task_ts(pre: SystemState, post: SystemState)
  requires
    true /* pre_ts_2 has no assertion */,
    ts_local_write_frame(pre, post),
    ts_global_write_frame(pre, post),
  ensures
    true /* post_ts_3 has no assertion */,
{}

/** VC[5]: Next-Assert (control point) -- START |- pre_oi_1, pre_ts_2 (state unchanged) */
pub proof fn vc_next_assert_skip_t2(st: SystemState)
  requires
    true /* START has no assertion */,
  ensures
    true /* pre_oi_1 has no assertion */,
    true /* pre_ts_2 has no assertion */,
{}

/** VC[6]: Next-Assert (control point) -- END_OI_Assert, post_ts_3 |- Post_TS_OI_Assert (state unchanged) */
pub proof fn vc_next_assert_skip_t3(st: SystemState)
  requires
    sys_assert_END_OI_Assert(st),
    true /* post_ts_3 has no assertion */,
  ensures
    sys_assert_Post_TS_OI_Assert(st),
{}

/** VC[7]: Pre-Assert -- START_Regulator_Assert |- DRF compute + integration assumes */
pub proof fn vc_pre_assert_drf(st: SystemState)
  requires
    sys_assert_START_Regulator_Assert(st),
  ensures
    true /* no assertions at out-places */,
{}

/** VC[8]: Next-Assert (task) -- START_Regulator_Assert + frames + DRF postcondition |- Post_DRF_Assert */
pub proof fn vc_next_assert_task_drf(pre: SystemState, post: SystemState)
  requires
    sys_assert_START_Regulator_Assert(pre),
    drf_local_write_frame(pre, post),
    drf_global_write_frame(pre, post),
  ensures
    sys_assert_Post_DRF_Assert(post),
{}

/** VC[9]: Pre-Assert -- Post_DRF_Assert |- MRI compute + integration assumes */
pub proof fn vc_pre_assert_mri(st: SystemState)
  requires
    sys_assert_Post_DRF_Assert(st),
  ensures
    mri::compute_spec_lower_is_not_higher_than_upper_assume(st.lower_desired_tempWstatus, st.upper_desired_tempWstatus),
{}

/** VC[10]: Next-Assert (task) -- Post_DRF_Assert + frames + MRI postcondition |- Post_MRI_Assert */
pub proof fn vc_next_assert_task_mri(pre: SystemState, post: SystemState)
  requires
    sys_assert_Post_DRF_Assert(pre),
    mri_local_write_frame(pre, post),
    mri_global_write_frame(pre, post),
    mri::compute_spec_lower_is_lower_temp_guarantee(post.lower_desired_temp, post.upper_desired_temp),
    mri::compute_case_REQ_MRI_1(pre.regulator_mode, post.regulator_status),
    mri::compute_case_REQ_MRI_2(pre.regulator_mode, post.regulator_status),
    mri::compute_case_REQ_MRI_3(pre.regulator_mode, post.regulator_status),
    mri::compute_case_REQ_MRI_4(pre.current_tempWstatus, pre.regulator_mode, post.displayed_temp),
    mri::compute_case_REQ_MRI_5(),
    mri::compute_case_REQ_MRI_6(pre.upper_desired_tempWstatus, post.reg_interface_failure),
    mri::compute_case_REQ_MRI_7(pre.lower_desired_tempWstatus, pre.upper_desired_tempWstatus, post.reg_interface_failure),
    mri::compute_case_REQ_MRI_8(pre.lower_desired_tempWstatus, pre.upper_desired_tempWstatus, post.reg_interface_failure, post.lower_desired_temp, post.upper_desired_temp),
    mri::compute_case_REQ_MRI_9(post.reg_interface_failure),
  ensures
    sys_assert_Post_MRI_Assert(post),
{}

/** VC[11]: Pre-Assert -- Post_MRI_Assert |- MRM compute + integration assumes */
pub proof fn vc_pre_assert_mrm(st: SystemState)
  requires
    sys_assert_Post_MRI_Assert(st),
  ensures
    true /* no assertions at out-places */,
{}

/** VC[12]: Next-Assert (task) -- Post_MRI_Assert + frames + MRM postcondition |- Post_MRM_Assert */
pub proof fn vc_next_assert_task_mrm(pre: SystemState, post: SystemState)
  requires
    sys_assert_Post_MRI_Assert(pre),
    mrm_local_write_frame(pre, post),
    mrm_global_write_frame(pre, post),
    mrm::compute_case_REQ_MRM_2(post.lastRegulatorMode, pre.current_tempWstatus, pre.reg_interface_failure, pre.internal_failure, post.regulator_mode),
    mrm::compute_case_REQ_MRM_Maintain_Normal(post.lastRegulatorMode, pre.current_tempWstatus, pre.reg_interface_failure, pre.internal_failure, post.regulator_mode),
    mrm::compute_case_REQ_MRM_3(post.lastRegulatorMode, pre.current_tempWstatus, pre.reg_interface_failure, pre.internal_failure, post.regulator_mode),
    mrm::compute_case_REQ_MRM_4(post.lastRegulatorMode, pre.current_tempWstatus, pre.reg_interface_failure, pre.internal_failure, post.regulator_mode),
    mrm::compute_case_REQ_MRM_MaintainFailed(post.lastRegulatorMode, post.regulator_mode),
  ensures
    sys_assert_Post_MRM_Assert(post),
{}

/** VC[13]: Pre-Assert -- Post_MRM_Assert |- MHS compute + integration assumes */
pub proof fn vc_pre_assert_mhs(st: SystemState)
  requires
    sys_assert_Post_MRM_Assert(st),
  ensures
    mhs::compute_spec_lower_is_lower_temp_assume(st.lower_desired_temp, st.upper_desired_temp),
{}

/** VC[14]: Next-Assert (task) -- Post_MRM_Assert + frames + MHS postcondition |- END_Regulator_Assert */
pub proof fn vc_next_assert_task_mhs(pre: SystemState, post: SystemState)
  requires
    sys_assert_Post_MRM_Assert(pre),
    mhs_local_write_frame(pre, post),
    mhs_global_write_frame(pre, post),
    mhs::compute_spec_lastCmd_guarantee(post.lastCmd, post.heat_control),
    mhs::compute_case_REQ_MHS_1(pre.regulator_mode, post.heat_control),
    mhs::compute_case_REQ_MHS_2(pre.current_tempWstatus, pre.lower_desired_temp, pre.regulator_mode, post.heat_control),
    mhs::compute_case_REQ_MHS_3(pre.current_tempWstatus, pre.regulator_mode, pre.upper_desired_temp, post.heat_control),
    mhs::compute_case_REQ_MHS_4(pre.lastCmd, pre.current_tempWstatus, pre.lower_desired_temp, pre.regulator_mode, pre.upper_desired_temp, post.heat_control),
    mhs::compute_case_REQ_MHS_5(pre.regulator_mode, post.heat_control),
  ensures
    sys_assert_END_Regulator_Assert(post),
{}

/** VC[15]: Pre-Assert -- START_Monitor_Assert |- DMF compute + integration assumes */
pub proof fn vc_pre_assert_dmf(st: SystemState)
  requires
    sys_assert_START_Monitor_Assert(st),
  ensures
    true /* no assertions at out-places */,
{}

/** VC[16]: Next-Assert (task) -- START_Monitor_Assert + frames + DMF postcondition |- Post_DMF_Assert */
pub proof fn vc_next_assert_task_dmf(pre: SystemState, post: SystemState)
  requires
    sys_assert_START_Monitor_Assert(pre),
    dmf_local_write_frame(pre, post),
    dmf_global_write_frame(pre, post),
  ensures
    sys_assert_Post_DMF_Assert(post),
{}

/** VC[17]: Pre-Assert -- Post_DMF_Assert |- MMI compute + integration assumes */
pub proof fn vc_pre_assert_mmi(st: SystemState)
  requires
    sys_assert_Post_DMF_Assert(st),
  ensures
    mmi::compute_spec_Allowed_AlarmTempWstatus_Ranges_assume(st.lower_alarm_tempWstatus, st.upper_alarm_tempWstatus),
    mmi::integration_spec_Allowed_UpperAlarmTemp_assume(st.upper_alarm_tempWstatus),
    mmi::integration_spec_Allowed_LowerAlarmTemp_assume(st.lower_alarm_tempWstatus),
{}

/** VC[18]: Next-Assert (task) -- Post_DMF_Assert + frames + MMI postcondition |- Post_MMI_Assert */
pub proof fn vc_next_assert_task_mmi(pre: SystemState, post: SystemState)
  requires
    sys_assert_Post_DMF_Assert(pre),
    mmi_local_write_frame(pre, post),
    mmi_global_write_frame(pre, post),
    mmi::compute_case_REQ_MMI_1(pre.monitor_mode, post.monitor_status),
    mmi::compute_case_REQ_MMI_2(pre.monitor_mode, post.monitor_status),
    mmi::compute_case_REQ_MMI_3(pre.monitor_mode, post.monitor_status),
    mmi::compute_case_REQ_MMI_4(pre.lower_alarm_tempWstatus, pre.upper_alarm_tempWstatus, post.mon_interface_failure),
    mmi::compute_case_REQ_MMI_5(pre.lower_alarm_tempWstatus, pre.upper_alarm_tempWstatus, post.mon_interface_failure),
    mmi::compute_case_REQ_MMI_6(pre.lower_alarm_tempWstatus, pre.upper_alarm_tempWstatus, post.mon_interface_failure, post.lower_alarm_temp, post.upper_alarm_temp),
    mmi::compute_case_REQ_MMI_7(post.mon_interface_failure),
  ensures
    sys_assert_Post_MMI_Assert(post),
{}

/** VC[19]: Pre-Assert -- Post_MMI_Assert |- MMM compute + integration assumes */
pub proof fn vc_pre_assert_mmm(st: SystemState)
  requires
    sys_assert_Post_MMI_Assert(st),
  ensures
    true /* no assertions at out-places */,
{}

/** VC[20]: Next-Assert (task) -- Post_MMI_Assert + frames + MMM postcondition |- Post_MMM_Assert */
pub proof fn vc_next_assert_task_mmm(pre: SystemState, post: SystemState)
  requires
    sys_assert_Post_MMI_Assert(pre),
    mmm_local_write_frame(pre, post),
    mmm_global_write_frame(pre, post),
    mmm::compute_case_REQ_MMM_2(post.lastMonitorMode, pre.current_tempWstatus, pre.mon_interface_failure, pre.dmf_internal_failure, post.monitor_mode),
    mmm::compute_case_REQ_MMM_3(post.lastMonitorMode, pre.current_tempWstatus, pre.mon_interface_failure, pre.dmf_internal_failure, post.monitor_mode),
    mmm::compute_case_REQ_MMM_4(post.lastMonitorMode, post.monitor_mode),
    mmm::compute_case_Failed_Mode_Absorbing(post.lastMonitorMode, post.monitor_mode),
  ensures
    sys_assert_Post_MMM_Assert(post),
{}

/** VC[21]: Pre-Assert -- Post_MMM_Assert |- MA compute + integration assumes */
pub proof fn vc_pre_assert_ma(st: SystemState)
  requires
    sys_assert_Post_MMM_Assert(st),
  ensures
    ma::compute_spec_Figure_A_7_assume(st.lower_alarm_temp, st.monitor_mode, st.upper_alarm_temp),
    ma::compute_spec_Table_A_12_LowerAlarmTemp_assume(st.lower_alarm_temp, st.monitor_mode),
    ma::compute_spec_Table_A_12_UpperAlarmTemp_assume(st.monitor_mode, st.upper_alarm_temp),
{}

/** VC[22]: Next-Assert (task) -- Post_MMM_Assert + frames + MA postcondition |- END_Monitor_Assert */
pub proof fn vc_next_assert_task_ma(pre: SystemState, post: SystemState)
  requires
    sys_assert_Post_MMM_Assert(pre),
    ma_local_write_frame(pre, post),
    ma_global_write_frame(pre, post),
    ma::compute_case_REQ_MA_1(post.ma_lastCmd, pre.monitor_mode, post.alarm_control),
    ma::compute_case_REQ_MA_2(post.ma_lastCmd, pre.current_tempWstatus, pre.lower_alarm_temp, pre.monitor_mode, pre.upper_alarm_temp, post.alarm_control),
    ma::compute_case_REQ_MA_3(pre.ma_lastCmd, post.ma_lastCmd, pre.current_tempWstatus, pre.lower_alarm_temp, pre.monitor_mode, pre.upper_alarm_temp, post.alarm_control),
    ma::compute_case_REQ_MA_4(post.ma_lastCmd, pre.current_tempWstatus, pre.lower_alarm_temp, pre.monitor_mode, pre.upper_alarm_temp, post.alarm_control),
    ma::compute_case_REQ_MA_5(post.ma_lastCmd, pre.monitor_mode, post.alarm_control),
  ensures
    sys_assert_END_Monitor_Assert(post),
{}

/** VC[23]: Next-Assert (control point) -- Post_TS_OI_Assert |- START_Regulator_Assert, START_Monitor_Assert (state unchanged) */
pub proof fn vc_next_assert_skip_t12(st: SystemState)
  requires
    sys_assert_Post_TS_OI_Assert(st),
  ensures
    sys_assert_START_Regulator_Assert(st),
    sys_assert_START_Monitor_Assert(st),
{}

/** VC[24]: Next-Assert (control point) -- END_Regulator_Assert, END_Monitor_Assert |- post_join_4 (state unchanged) */
pub proof fn vc_next_assert_skip_t13(st: SystemState)
  requires
    sys_assert_END_Regulator_Assert(st),
    sys_assert_END_Monitor_Assert(st),
  ensures
    true /* post_join_4 has no assertion */,
{}

/** VC[25]: Pre-Assert -- post_join_4 |- HS compute + integration assumes */
pub proof fn vc_pre_assert_hs(st: SystemState)
  requires
    true /* post_join_4 has no assertion */,
  ensures
    true /* no assertions at out-places */,
{}

/** VC[26]: Next-Assert (task) -- post_join_4 + frames + HS postcondition |- post_hs_5 */
pub proof fn vc_next_assert_task_hs(pre: SystemState, post: SystemState)
  requires
    true /* post_join_4 has no assertion */,
    hs_local_write_frame(pre, post),
    hs_global_write_frame(pre, post),
  ensures
    true /* post_hs_5 has no assertion */,
{}

/** VC[27]: Next-Assert (control point) -- post_hs_5 |- END (state unchanged) */
pub proof fn vc_next_assert_skip_t15(st: SystemState)
  requires
    true /* post_hs_5 has no assertion */,
  ensures
    true /* END has no assertion */,
{}

} // verus!
