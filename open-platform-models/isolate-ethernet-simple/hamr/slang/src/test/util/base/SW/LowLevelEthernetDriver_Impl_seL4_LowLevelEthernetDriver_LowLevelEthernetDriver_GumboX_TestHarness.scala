// #Sireum

package base.SW

import org.sireum._
import base._
import base.GumboXUtil.GumboXResult

// Do not edit this file as it will be overwritten if HAMR codegen is rerun
@msig trait LowLevelEthernetDriver_Impl_seL4_LowLevelEthernetDriver_LowLevelEthernetDriver_GumboX_TestHarness extends LowLevelEthernetDriver_Impl_seL4_LowLevelEthernetDriver_LowLevelEthernetDriver_TestApi {
  def verbose: B

  /** Contract-based test harness for the initialise entry point
    */
  def testInitialiseCB(
      ): GumboXResult.Type = {

    if (verbose) {
      println(st"""Pre State Values:
                  """.render)
    }

    // [InvokeEntryPoint]: invoke the entry point test method
    testInitialise()

    // [RetrieveOutState]: retrieve values of the output ports via get operations and GUMBO declared local state variable
    val api_EthernetFramesRx: Option[SW.StructuredEthernetMessage_i] = get_EthernetFramesRx()

    if (verbose) {
      println(st"""Post State Values:
                  |  api_EthernetFramesRx = ${api_EthernetFramesRx.string}""".render)
    }

    // [CheckPost]: invoke the oracle function
    val postResult = base.SW.LowLevelEthernetDriver_Impl_seL4_LowLevelEthernetDriver_LowLevelEthernetDriver_GumboX.inititialize_IEP_Post(api_EthernetFramesRx)
    val result: GumboXResult.Type =
      if (!postResult) GumboXResult.Post_Condition_Fail
      else GumboXResult.Post_Condition_Pass

    return result
  }

  def testComputeCBJ(json: String): GumboXResult.Type = {
    base.JSON.toSWLowLevelEthernetDriver_Impl_seL4_LowLevelEthernetDriver_LowLevelEthernetDriver_PreState_Container(json) match {
      case Either.Left(o) => return testComputeCBV(o)
      case Either.Right(msg) => halt(msg.string)
    }
  }

  def testComputeCBV(o: LowLevelEthernetDriver_Impl_seL4_LowLevelEthernetDriver_LowLevelEthernetDriver_PreState_Container): GumboXResult.Type = {
    return testComputeCB(o.api_EthernetFramesTx)
  }

  /** Contract-based test harness for the compute entry point
    * @param api_EthernetFramesTx incoming event data port
    */
  def testComputeCB(
      api_EthernetFramesTx: Option[SW.StructuredEthernetMessage_i]): GumboXResult.Type = {

    // [SaveInLocal]: retrieve and save the current (input) values of GUMBO-declared local state variables as retrieved from the component state
    //   LowLevelEthernetDriver does not have incoming ports or state variables

    // [CheckPre]: check/filter based on pre-condition.
    val CEP_Pre_Result: B = base.SW.LowLevelEthernetDriver_Impl_seL4_LowLevelEthernetDriver_LowLevelEthernetDriver_GumboX.compute_CEP_Pre (api_EthernetFramesTx)
    if (!CEP_Pre_Result) {
      return GumboXResult.Pre_Condition_Unsat
    }

    // [PutInPorts]: put values on the input ports
    if (api_EthernetFramesTx.nonEmpty) {
      put_EthernetFramesTx(api_EthernetFramesTx.get)
    }

    if (verbose) {
      println(st"""Pre State Values:
                  |  api_EthernetFramesTx = ${api_EthernetFramesTx.string}""".render)
    }

    // [InvokeEntryPoint]: invoke the entry point test method
    testCompute()

    // [RetrieveOutState]: retrieve values of the output ports via get operations and GUMBO declared local state variable
    val api_EthernetFramesRx: Option[SW.StructuredEthernetMessage_i] = get_EthernetFramesRx()

    if (verbose) {
      println(st"""Post State Values:
                  |  api_EthernetFramesRx = ${api_EthernetFramesRx.string}""".render)
    }

    // [CheckPost]: invoke the oracle function
    val postResult = base.SW.LowLevelEthernetDriver_Impl_seL4_LowLevelEthernetDriver_LowLevelEthernetDriver_GumboX.compute_CEP_Post(api_EthernetFramesTx, api_EthernetFramesRx)
    val result: GumboXResult.Type =
      if (!postResult) GumboXResult.Post_Condition_Fail
      else GumboXResult.Post_Condition_Pass

    return result
  }
}
