// Do not edit this file as it will be overwritten if codegen is rerun

//! C-interface for the component.
//! This code must be unsafe.
//! Assumptions about correctness are introduced and need to be verified by other means.

use data::*;

#[cfg(test)]
use std::sync::Mutex;

#[cfg(not(test))]
extern "C" {
  fn get_EthernetFramesRxIn(value: *mut SW::StructuredEthernetMessage_i) -> bool;
  fn get_EthernetFramesTxIn(value: *mut SW::StructuredEthernetMessage_i) -> bool;
  fn put_EthernetFramesRxOut(value: *mut SW::StructuredEthernetMessage_i) -> bool;
  fn put_EthernetFramesTxOut(value: *mut SW::StructuredEthernetMessage_i) -> bool;
}

pub fn unsafe_get_EthernetFramesRxIn() -> Option<SW::StructuredEthernetMessage_i>
{
  unsafe {
    let value: *mut SW::StructuredEthernetMessage_i = &mut SW::StructuredEthernetMessage_i::default();
    if (get_EthernetFramesRxIn(value)) {
      return Some(*value);
    } else {
      return None;
    }
  }
}

pub fn unsafe_get_EthernetFramesTxIn() -> Option<SW::StructuredEthernetMessage_i>
{
  unsafe {
    let value: *mut SW::StructuredEthernetMessage_i = &mut SW::StructuredEthernetMessage_i::default();
    if (get_EthernetFramesTxIn(value)) {
      return Some(*value);
    } else {
      return None;
    }
  }
}

pub fn unsafe_put_EthernetFramesRxOut(value: &SW::StructuredEthernetMessage_i) -> bool
{
  unsafe {
    return put_EthernetFramesRxOut(value as *const SW::StructuredEthernetMessage_i as *mut SW::StructuredEthernetMessage_i);
  }
}

pub fn unsafe_put_EthernetFramesTxOut(value: &SW::StructuredEthernetMessage_i) -> bool
{
  unsafe {
    return put_EthernetFramesTxOut(value as *const SW::StructuredEthernetMessage_i as *mut SW::StructuredEthernetMessage_i);
  }
}

//////////////////////////////////////////////////////////////////////////////////
// Testing Versions
//////////////////////////////////////////////////////////////////////////////////

#[cfg(test)]
lazy_static::lazy_static! {
  // simulate the global C variables that point to the microkit shared memory regions.  In a full
  // microkit system we would be able to mutate the shared memory for out ports since they're r/w,
  // but we couldn't do that for in ports since they are read-only
  pub static ref IN_EthernetFramesRxIn: Mutex<Option<SW::StructuredEthernetMessage_i>> = Mutex::new(None);
  pub static ref IN_EthernetFramesTxIn: Mutex<Option<SW::StructuredEthernetMessage_i>> = Mutex::new(None);
  pub static ref OUT_EthernetFramesRxOut: Mutex<Option<SW::StructuredEthernetMessage_i>> = Mutex::new(None);
  pub static ref OUT_EthernetFramesTxOut: Mutex<Option<SW::StructuredEthernetMessage_i>> = Mutex::new(None);
}

#[cfg(test)]
pub fn initialize_test_globals() {
  unsafe {
    *IN_EthernetFramesRxIn.lock().unwrap_or_else(|e| e.into_inner()) = None;
    *IN_EthernetFramesTxIn.lock().unwrap_or_else(|e| e.into_inner()) = None;
    *OUT_EthernetFramesRxOut.lock().unwrap_or_else(|e| e.into_inner()) = None;
    *OUT_EthernetFramesTxOut.lock().unwrap_or_else(|e| e.into_inner()) = None;
  }
}

#[cfg(test)]
pub fn get_EthernetFramesRxIn(value: *mut SW::StructuredEthernetMessage_i) -> bool
{
  unsafe {
    match *IN_EthernetFramesRxIn.lock().unwrap_or_else(|e| e.into_inner()) {
      Some(v) => {
        *value = v;
        return true;
      },
      None => return false,
    }
  }
}

#[cfg(test)]
pub fn get_EthernetFramesTxIn(value: *mut SW::StructuredEthernetMessage_i) -> bool
{
  unsafe {
    match *IN_EthernetFramesTxIn.lock().unwrap_or_else(|e| e.into_inner()) {
      Some(v) => {
        *value = v;
        return true;
      },
      None => return false,
    }
  }
}

#[cfg(test)]
pub fn put_EthernetFramesRxOut(value: *mut SW::StructuredEthernetMessage_i) -> bool
{
  unsafe {
    *OUT_EthernetFramesRxOut.lock().unwrap_or_else(|e| e.into_inner()) = Some(*value);
    return true;
  }
}

#[cfg(test)]
pub fn put_EthernetFramesTxOut(value: *mut SW::StructuredEthernetMessage_i) -> bool
{
  unsafe {
    *OUT_EthernetFramesTxOut.lock().unwrap_or_else(|e| e.into_inner()) = Some(*value);
    return true;
  }
}
