// Do not edit this file as it will be overwritten if codegen is rerun

//! C-interface for the component.
//! This code must be unsafe.
//! Assumptions about correctness are introduced and need to be verified by other means.

use data::*;

#[cfg(test)]
use std::sync::Mutex;

#[cfg(not(test))]
extern "C" {
  fn get_ArduPilot_ArduPilot_EthernetFramesTx(value: *mut SW::RawEthernetMessage) -> bool;
  fn get_Firewall_Firewall_EthernetFramesRxOut(value: *mut SW::RawEthernetMessage) -> bool;
  fn get_Firewall_Firewall_EthernetFramesTxOut(value: *mut SW::RawEthernetMessage) -> bool;
  fn get_LowLevelEthernetDriver_LowLevelEthernetDriver_EthernetFramesRx(value: *mut SW::RawEthernetMessage) -> bool;
}

pub fn unsafe_get_ArduPilot_ArduPilot_EthernetFramesTx() -> Option<SW::RawEthernetMessage>
{
  unsafe {
    let value: *mut SW::RawEthernetMessage = &mut [0; SW::SW_RawEthernetMessage_DIM_0];
    if (get_ArduPilot_ArduPilot_EthernetFramesTx(value)) {
      return Some(*value);
    } else {
      return None;
    }
  }
}

pub fn unsafe_get_Firewall_Firewall_EthernetFramesRxOut() -> Option<SW::RawEthernetMessage>
{
  unsafe {
    let value: *mut SW::RawEthernetMessage = &mut [0; SW::SW_RawEthernetMessage_DIM_0];
    if (get_Firewall_Firewall_EthernetFramesRxOut(value)) {
      return Some(*value);
    } else {
      return None;
    }
  }
}

pub fn unsafe_get_Firewall_Firewall_EthernetFramesTxOut() -> Option<SW::RawEthernetMessage>
{
  unsafe {
    let value: *mut SW::RawEthernetMessage = &mut [0; SW::SW_RawEthernetMessage_DIM_0];
    if (get_Firewall_Firewall_EthernetFramesTxOut(value)) {
      return Some(*value);
    } else {
      return None;
    }
  }
}

pub fn unsafe_get_LowLevelEthernetDriver_LowLevelEthernetDriver_EthernetFramesRx() -> Option<SW::RawEthernetMessage>
{
  unsafe {
    let value: *mut SW::RawEthernetMessage = &mut [0; SW::SW_RawEthernetMessage_DIM_0];
    if (get_LowLevelEthernetDriver_LowLevelEthernetDriver_EthernetFramesRx(value)) {
      return Some(*value);
    } else {
      return None;
    }
  }
}

//////////////////////////////////////////////////////////////////////////////////
// Testing Versions
//////////////////////////////////////////////////////////////////////////////////

#[cfg(test)]
lazy_static::lazy_static! {
  // simulate the global C variables that point to the microkit shared memory regions.  In a full
  // microkit system we would be able to mutate the shared memory for out ports since they're r/w,
  // but we couldn't do that for in ports since they are read-only
  pub static ref IN_ArduPilot_ArduPilot_EthernetFramesTx: Mutex<Option<SW::RawEthernetMessage>> = Mutex::new(None);
  pub static ref IN_Firewall_Firewall_EthernetFramesRxOut: Mutex<Option<SW::RawEthernetMessage>> = Mutex::new(None);
  pub static ref IN_Firewall_Firewall_EthernetFramesTxOut: Mutex<Option<SW::RawEthernetMessage>> = Mutex::new(None);
  pub static ref IN_LowLevelEthernetDriver_LowLevelEthernetDriver_EthernetFramesRx: Mutex<Option<SW::RawEthernetMessage>> = Mutex::new(None);
}

#[cfg(test)]
pub fn initialize_test_globals() {
  unsafe {
    *IN_ArduPilot_ArduPilot_EthernetFramesTx.lock().unwrap_or_else(|e| e.into_inner()) = None;
    *IN_Firewall_Firewall_EthernetFramesRxOut.lock().unwrap_or_else(|e| e.into_inner()) = None;
    *IN_Firewall_Firewall_EthernetFramesTxOut.lock().unwrap_or_else(|e| e.into_inner()) = None;
    *IN_LowLevelEthernetDriver_LowLevelEthernetDriver_EthernetFramesRx.lock().unwrap_or_else(|e| e.into_inner()) = None;
  }
}

#[cfg(test)]
pub fn get_ArduPilot_ArduPilot_EthernetFramesTx(value: *mut SW::RawEthernetMessage) -> bool
{
  unsafe {
    match *IN_ArduPilot_ArduPilot_EthernetFramesTx.lock().unwrap_or_else(|e| e.into_inner()) {
      Some(v) => {
        *value = v;
        return true;
      },
      None => return false,
    }
  }
}

#[cfg(test)]
pub fn get_Firewall_Firewall_EthernetFramesRxOut(value: *mut SW::RawEthernetMessage) -> bool
{
  unsafe {
    match *IN_Firewall_Firewall_EthernetFramesRxOut.lock().unwrap_or_else(|e| e.into_inner()) {
      Some(v) => {
        *value = v;
        return true;
      },
      None => return false,
    }
  }
}

#[cfg(test)]
pub fn get_Firewall_Firewall_EthernetFramesTxOut(value: *mut SW::RawEthernetMessage) -> bool
{
  unsafe {
    match *IN_Firewall_Firewall_EthernetFramesTxOut.lock().unwrap_or_else(|e| e.into_inner()) {
      Some(v) => {
        *value = v;
        return true;
      },
      None => return false,
    }
  }
}

#[cfg(test)]
pub fn get_LowLevelEthernetDriver_LowLevelEthernetDriver_EthernetFramesRx(value: *mut SW::RawEthernetMessage) -> bool
{
  unsafe {
    match *IN_LowLevelEthernetDriver_LowLevelEthernetDriver_EthernetFramesRx.lock().unwrap_or_else(|e| e.into_inner()) {
      Some(v) => {
        *value = v;
        return true;
      },
      None => return false,
    }
  }
}
