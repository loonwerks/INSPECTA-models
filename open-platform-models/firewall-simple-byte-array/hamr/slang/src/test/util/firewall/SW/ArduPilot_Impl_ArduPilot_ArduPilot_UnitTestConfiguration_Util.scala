// #Sireum

package firewall.SW

// Do not edit this file as it will be overwritten if HAMR codegen is rerun

import org.sireum._
import firewall.GumboXUtil.GumboXResult
import firewall.util.{Container, Profile, UnitTestConfigurationBatch}
import firewall.RandomLib
import org.sireum.Random.Impl.Xoshiro256

object ArduPilot_Impl_ArduPilot_ArduPilot_UnitTestConfiguration_Util {

  def freshRandomLib: RandomLib = {
    return RandomLib(Random.Gen64Impl(Xoshiro256.create))
  }

  val tq: String = "\"\"\""

  type DefaultInitializeProfile = ArduPilot_Impl_ArduPilot_ArduPilot_Profile

  def defaultInitializeConfig: ArduPilot_Impl_ArduPilot_ArduPilot_Initialize_UnitTestConfiguration = {
    return (ArduPilot_Impl_ArduPilot_ArduPilot_Initialize_UnitTestConfiguration (
      verbose = F,
      name = "Default_Initialize_Config",
      description = "Default Initialize Configuration",
      numTests = 100,
      numTestVectorGenRetries = 100,
      failOnUnsatPreconditions = F,
      profile = ArduPilot_Impl_ArduPilot_ArduPilot_Profile (
        name = "Initialize_Default_Profile",
      ),
      genReplay = (c: Container, testName: String, r: GumboXResult.Type) => None())
    )
  }

  type DefaultComputeProfile = ArduPilot_Impl_ArduPilot_ArduPilot_Profile_P

  def defaultComputeConfig: ArduPilot_Impl_ArduPilot_ArduPilot_Compute_UnitTestConfiguration = {
    return (ArduPilot_Impl_ArduPilot_ArduPilot_Compute_UnitTestConfiguration (
      verbose = F,
      name = "Default_Compute_Config",
      description = "Default Compute Configuration",
      numTests = 100,
      numTestVectorGenRetries = 100,
      failOnUnsatPreconditions = F,
      profile = ArduPilot_Impl_ArduPilot_ArduPilot_Profile_P (
        name = "Compute_Default_Profile",
        api_EthernetFramesRx = freshRandomLib
      ),
      genReplay = (c: Container, testName: String, r: GumboXResult.Type) => Some(
       st"""Replay Unit Test:
            |  test("Replay: $testName") {
            |    val results = firewall.GumboXUtil.GumboXResult.$r
            |    val json = st${tq}${firewall.JSON.fromutilContainer(c, T)}${tq}.render
            |    val testVector = firewall.JSON.toSWArduPilot_Impl_ArduPilot_ArduPilot_PreState_Container_P(json).left
            |    assert (testComputeCBV(testVector) == results)
            |  }""".render))
    )
  }
}

@record class ArduPilot_Impl_ArduPilot_ArduPilot_Initialize_UnitTestConfiguration (
  var verbose: B,
  var name: String,
  var description: String,
  var numTests: Z,
  var numTestVectorGenRetries: Z,
  var failOnUnsatPreconditions: B,
  var profile: ArduPilot_Impl_ArduPilot_ArduPilot_Profile_Trait,
  var genReplay: (Container, String, GumboXResult.Type) => Option[String])
  extends UnitTestConfigurationBatch with ArduPilot_Impl_ArduPilot_ArduPilot_GumboX_TestHarness {

  override def test(c: Container): GumboXResult.Type = {
    return testInitialiseCB()
  }
}

@record class ArduPilot_Impl_ArduPilot_ArduPilot_Compute_UnitTestConfiguration (
  var verbose: B,
  var name: String,
  var description: String,
  var numTests: Z,
  var numTestVectorGenRetries: Z,
  var failOnUnsatPreconditions: B,
  var profile: ArduPilot_Impl_ArduPilot_ArduPilot_Profile_P_Trait,
  var genReplay: (Container, String, GumboXResult.Type) => Option[String])
  extends UnitTestConfigurationBatch with ArduPilot_Impl_ArduPilot_ArduPilot_GumboX_TestHarness {

  override def test(c: Container): GumboXResult.Type = {
    return testComputeCBV(c.asInstanceOf[ArduPilot_Impl_ArduPilot_ArduPilot_PreState_Container])
  }
}
