// #Sireum

package firewall.SW

import org.sireum._
import art._
import firewall.SystemTestSuiteSlang.runtimeMonitorStream
import firewall._

// Do not edit this file as it will be overwritten if HAMR codegen is rerun

object LowLevelEthernetDriver_Impl_LowLevelEthernetDriver_LowLevelEthernetDriver_SystemTestAPI {
  /** helper method to set the values of all incoming ports
    * @param api_EthernetFramesTx incoming event data port
    */
  def put_concrete_inputs(api_EthernetFramesTx: Option[SW.RawEthernetMessage]): Unit = {
    put_EthernetFramesTx(api_EthernetFramesTx)
  }

  // setter for incoming event data port
  def put_EthernetFramesTx(value: Option[SW.RawEthernetMessage]): Unit = {
    value match {
      case Some(v) => Art.insertInInfrastructurePort(Arch.seL4_Impl_Instance_LowLevelEthernetDriver_LowLevelEthernetDriver.operational_api.EthernetFramesTx_Id, SW.RawEthernetMessage_Payload(v))
      case _ =>
    }
  }

  def fetchContainer(): firewall.SW.LowLevelEthernetDriver_Impl_LowLevelEthernetDriver_LowLevelEthernetDriver_PostState_Container_PS = {
    if (runtimeMonitorStream.contains(Arch.seL4_Impl_Instance_LowLevelEthernetDriver_LowLevelEthernetDriver.id)) {
      val (_, postContainer_) = runtimeMonitorStream.get(Arch.seL4_Impl_Instance_LowLevelEthernetDriver_LowLevelEthernetDriver.id).get
      return postContainer_.asInstanceOf[firewall.SW.LowLevelEthernetDriver_Impl_LowLevelEthernetDriver_LowLevelEthernetDriver_PostState_Container_PS]
    }
    else {
      assert(F, s"No post state recorded for ${Arch.seL4_Impl_Instance_LowLevelEthernetDriver_LowLevelEthernetDriver.name}")
      halt(s"No post state recorded for ${Arch.seL4_Impl_Instance_LowLevelEthernetDriver_LowLevelEthernetDriver.name}")
    }
  }

  def check_concrete_outputs(api_EthernetFramesRx: Option[SW.RawEthernetMessage]): Unit = {
    var failureReasons: ISZ[ST] = ISZ()

    val actual_EthernetFramesRx = get_api_EthernetFramesRx()
    if (api_EthernetFramesRx != actual_EthernetFramesRx) {
      failureReasons = failureReasons :+ st"'EthernetFramesRx' did not match expected.  Expected: $api_EthernetFramesRx, Actual: $actual_EthernetFramesRx"
    }

    assert(failureReasons.isEmpty, st"${(failureReasons, "\n")}".render)
  }

  def get_api_EthernetFramesRx(): Option[SW.RawEthernetMessage] = {
    return fetchContainer().api_EthernetFramesRx
  }
}