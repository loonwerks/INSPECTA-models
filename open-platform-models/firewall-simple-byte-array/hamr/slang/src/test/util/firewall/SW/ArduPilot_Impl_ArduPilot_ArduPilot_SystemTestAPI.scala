// #Sireum

package firewall.SW

import org.sireum._
import art._
import firewall.SystemTestSuiteSlang.runtimeMonitorStream
import firewall._

// Do not edit this file as it will be overwritten if HAMR codegen is rerun

object ArduPilot_Impl_ArduPilot_ArduPilot_SystemTestAPI {
  /** helper method to set the values of all incoming ports
    * @param api_EthernetFramesRx incoming event data port
    */
  def put_concrete_inputs(api_EthernetFramesRx: Option[SW.RawEthernetMessage]): Unit = {
    put_EthernetFramesRx(api_EthernetFramesRx)
  }

  // setter for incoming event data port
  def put_EthernetFramesRx(value: Option[SW.RawEthernetMessage]): Unit = {
    value match {
      case Some(v) => Art.insertInInfrastructurePort(Arch.seL4_Impl_Instance_ArduPilot_ArduPilot.operational_api.EthernetFramesRx_Id, SW.RawEthernetMessage_Payload(v))
      case _ =>
    }
  }

  def fetchContainer(): firewall.SW.ArduPilot_Impl_ArduPilot_ArduPilot_PostState_Container_PS = {
    if (runtimeMonitorStream.contains(Arch.seL4_Impl_Instance_ArduPilot_ArduPilot.id)) {
      val (_, postContainer_) = runtimeMonitorStream.get(Arch.seL4_Impl_Instance_ArduPilot_ArduPilot.id).get
      return postContainer_.asInstanceOf[firewall.SW.ArduPilot_Impl_ArduPilot_ArduPilot_PostState_Container_PS]
    }
    else {
      assert(F, s"No post state recorded for ${Arch.seL4_Impl_Instance_ArduPilot_ArduPilot.name}")
      halt(s"No post state recorded for ${Arch.seL4_Impl_Instance_ArduPilot_ArduPilot.name}")
    }
  }

  def check_concrete_outputs(api_EthernetFramesTx: Option[SW.RawEthernetMessage]): Unit = {
    var failureReasons: ISZ[ST] = ISZ()

    val actual_EthernetFramesTx = get_api_EthernetFramesTx()
    if (api_EthernetFramesTx != actual_EthernetFramesTx) {
      failureReasons = failureReasons :+ st"'EthernetFramesTx' did not match expected.  Expected: $api_EthernetFramesTx, Actual: $actual_EthernetFramesTx"
    }

    assert(failureReasons.isEmpty, st"${(failureReasons, "\n")}".render)
  }

  def get_api_EthernetFramesTx(): Option[SW.RawEthernetMessage] = {
    return fetchContainer().api_EthernetFramesTx
  }
}