// Do not edit this file as it will be overwritten if codegen is rerun

//! C-interface for the component.
//! This code must be unsafe.
//! Assumptions about correctness are introduced and need to be verified by other means.

use data::*;

#[cfg(test)]
use std::sync::Mutex;

#[cfg(not(test))]
extern "C" {
  fn get_p1_t1_write_port(value: *mut i32) -> bool;
  fn get_p2_t2_write_port(value: *mut i32) -> bool;
}

pub fn unsafe_get_p1_t1_write_port() -> i32
{
  unsafe {
    let value: *mut i32 = &mut 0;
    get_p1_t1_write_port(value);
    return *value;
  }
}

pub fn unsafe_get_p2_t2_write_port() -> i32
{
  unsafe {
    let value: *mut i32 = &mut 0;
    get_p2_t2_write_port(value);
    return *value;
  }
}

//////////////////////////////////////////////////////////////////////////////////
// Testing Versions
//////////////////////////////////////////////////////////////////////////////////

#[cfg(test)]
lazy_static::lazy_static! {
  // simulate the global C variables that point to the microkit shared memory regions.  In a full
  // microkit system we would be able to mutate the shared memory for out ports since they're r/w,
  // but we couldn't do that for in ports since they are read-only
  pub static ref IN_p1_t1_write_port: Mutex<Option<i32>> = Mutex::new(None);
  pub static ref IN_p2_t2_write_port: Mutex<Option<i32>> = Mutex::new(None);
}

#[cfg(test)]
pub fn initialize_test_globals() {
  unsafe {
    *IN_p1_t1_write_port.lock().unwrap_or_else(|e| e.into_inner()) = None;
    *IN_p2_t2_write_port.lock().unwrap_or_else(|e| e.into_inner()) = None;
  }
}

#[cfg(test)]
pub fn get_p1_t1_write_port(value: *mut i32) -> bool
{
  unsafe {
    let guard = IN_p1_t1_write_port.lock().unwrap_or_else(|e| e.into_inner());
    *value = guard.expect("Not expecting None");
    true
  }
}

#[cfg(test)]
pub fn get_p2_t2_write_port(value: *mut i32) -> bool
{
  unsafe {
    let guard = IN_p2_t2_write_port.lock().unwrap_or_else(|e| e.into_inner());
    *value = guard.expect("Not expecting None");
    true
  }
}
