// Do not edit this file as it will be overwritten if HAMR codegen is rerun

use crate::bridge::extern_c_api as extern_api;
use data::*;

use proptest::prelude::*;

/// container for component's incoming port values
pub struct PreStateContainer {
  pub api_currentTemp: Option<TempControl_SysVerif::Temperature>,
  pub api_fanAck: Option<TempControl_SysVerif::FanAck>,
  pub api_setPoint: Option<TempControl_SysVerif::SetPoint>
}

/// container for component's incoming port values and GUMBO state variables
pub struct PreStateContainer_wGSV {
  pub In_currentSetPoint: TempControl_SysVerif::SetPoint,
  pub In_currentFanState: TempControl_SysVerif::FanCmd,
  pub In_latestTemp: TempControl_SysVerif::Temperature,
  pub In_fanError: bool,
  pub api_currentTemp: Option<TempControl_SysVerif::Temperature>,
  pub api_fanAck: Option<TempControl_SysVerif::FanAck>,
  pub api_setPoint: Option<TempControl_SysVerif::SetPoint>
}

/// setter for component's incoming port values
pub fn put_concrete_inputs_container(container: PreStateContainer)
{
  put_currentTemp(container.api_currentTemp);
  put_fanAck(container.api_fanAck);
  put_setPoint(container.api_setPoint);
}

/// setter for component's incoming port values and GUMBO state variables
pub fn put_concrete_inputs_container_wGSV(container: PreStateContainer_wGSV)
{
  put_fanError(container.In_fanError);
  put_latestTemp(container.In_latestTemp);
  put_currentFanState(container.In_currentFanState);
  put_currentSetPoint(container.In_currentSetPoint);
  put_currentTemp(container.api_currentTemp);
  put_fanAck(container.api_fanAck);
  put_setPoint(container.api_setPoint);
}

/// setter for component's incoming port values
pub fn put_concrete_inputs(
  currentTemp: Option<TempControl_SysVerif::Temperature>,
  fanAck: Option<TempControl_SysVerif::FanAck>,
  setPoint: Option<TempControl_SysVerif::SetPoint>)
{
  put_currentTemp(currentTemp);
  put_fanAck(fanAck);
  put_setPoint(setPoint);
}

/// setter for component's incoming port values and GUMBO state variables
pub fn put_concrete_inputs_wGSV(
  In_currentSetPoint: TempControl_SysVerif::SetPoint,
  In_currentFanState: TempControl_SysVerif::FanCmd,
  In_latestTemp: TempControl_SysVerif::Temperature,
  In_fanError: bool,
  currentTemp: Option<TempControl_SysVerif::Temperature>,
  fanAck: Option<TempControl_SysVerif::FanAck>,
  setPoint: Option<TempControl_SysVerif::SetPoint>)
{
  put_fanError(In_fanError);
  put_latestTemp(In_latestTemp);
  put_currentFanState(In_currentFanState);
  put_currentSetPoint(In_currentSetPoint);
  put_currentTemp(currentTemp);
  put_fanAck(fanAck);
  put_setPoint(setPoint);
}

/// setter for IN EventDataPort
pub fn put_currentTemp(value: Option<TempControl_SysVerif::Temperature>)
{
  *extern_api::IN_currentTemp.lock().unwrap_or_else(|e| e.into_inner()) = value
}

/// setter for IN EventDataPort
pub fn put_fanAck(value: Option<TempControl_SysVerif::FanAck>)
{
  *extern_api::IN_fanAck.lock().unwrap_or_else(|e| e.into_inner()) = value
}

/// setter for IN EventDataPort
pub fn put_setPoint(value: Option<TempControl_SysVerif::SetPoint>)
{
  *extern_api::IN_setPoint.lock().unwrap_or_else(|e| e.into_inner()) = value
}

/// getter for OUT EventDataPort
pub fn get_fanCmd() -> Option<TempControl_SysVerif::FanCmd>
{
  return extern_api::OUT_fanCmd.lock().unwrap_or_else(|e| e.into_inner()).clone()
}

/// getter for OUT DataPort
pub fn get_sv_currentSetPoint() -> TempControl_SysVerif::SetPoint
{
  return extern_api::OUT_sv_currentSetPoint.lock().unwrap_or_else(|e| e.into_inner()).expect("Not expecting None")
}

/// getter for OUT DataPort
pub fn get_sv_currentFanState() -> TempControl_SysVerif::FanCmd
{
  return extern_api::OUT_sv_currentFanState.lock().unwrap_or_else(|e| e.into_inner()).expect("Not expecting None")
}

/// getter for OUT DataPort
pub fn get_sv_latestTemp() -> TempControl_SysVerif::Temperature
{
  return extern_api::OUT_sv_latestTemp.lock().unwrap_or_else(|e| e.into_inner()).expect("Not expecting None")
}

/// getter for OUT DataPort
pub fn get_sv_fanError() -> bool
{
  return extern_api::OUT_sv_fanError.lock().unwrap_or_else(|e| e.into_inner()).expect("Not expecting None")
}

/// getter for GUMBO State Variable
pub fn get_currentSetPoint() -> TempControl_SysVerif::SetPoint
{
  unsafe {
    match &crate::app {
      Some(inner) => inner.currentSetPoint,
      None => panic!("The app is None")
    }
  }
}

/// setter for GUMBO State Variable
pub fn put_currentSetPoint(value: TempControl_SysVerif::SetPoint)
{
  unsafe {
    match &mut crate::app {
      Some(inner) => inner.currentSetPoint = value,
      None => panic!("The app is None")
    }
  }
}

/// getter for GUMBO State Variable
pub fn get_currentFanState() -> TempControl_SysVerif::FanCmd
{
  unsafe {
    match &crate::app {
      Some(inner) => inner.currentFanState,
      None => panic!("The app is None")
    }
  }
}

/// setter for GUMBO State Variable
pub fn put_currentFanState(value: TempControl_SysVerif::FanCmd)
{
  unsafe {
    match &mut crate::app {
      Some(inner) => inner.currentFanState = value,
      None => panic!("The app is None")
    }
  }
}

/// getter for GUMBO State Variable
pub fn get_latestTemp() -> TempControl_SysVerif::Temperature
{
  unsafe {
    match &crate::app {
      Some(inner) => inner.latestTemp,
      None => panic!("The app is None")
    }
  }
}

/// setter for GUMBO State Variable
pub fn put_latestTemp(value: TempControl_SysVerif::Temperature)
{
  unsafe {
    match &mut crate::app {
      Some(inner) => inner.latestTemp = value,
      None => panic!("The app is None")
    }
  }
}

/// getter for GUMBO State Variable
pub fn get_fanError() -> bool
{
  unsafe {
    match &crate::app {
      Some(inner) => inner.fanError,
      None => panic!("The app is None")
    }
  }
}

/// setter for GUMBO State Variable
pub fn put_fanError(value: bool)
{
  unsafe {
    match &mut crate::app {
      Some(inner) => inner.fanError = value,
      None => panic!("The app is None")
    }
  }
}
