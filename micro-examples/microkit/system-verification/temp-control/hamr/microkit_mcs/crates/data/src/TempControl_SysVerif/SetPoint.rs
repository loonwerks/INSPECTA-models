// Do not edit this file as it will be overwritten if HAMR codegen is rerun

use vstd::prelude::*;

use super::*;

verus! {

  #[repr(C)]
  #[derive(Debug, Clone, Copy, PartialEq, Eq)]
  pub struct SetPoint {
    pub low: Temperature,
    pub high: Temperature
  }

  impl Default for SetPoint {
    fn default() -> Self
    {
      Self { low: TempControl_SysVerif::Temperature::default(), high: TempControl_SysVerif::Temperature::default() }
    }
  }

}