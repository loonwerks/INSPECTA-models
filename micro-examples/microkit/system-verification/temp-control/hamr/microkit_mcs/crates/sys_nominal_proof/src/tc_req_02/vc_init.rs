// Do not edit this file as it will be overwritten if HAMR codegen is rerun

//! Property tc_req_02 -- Init-State VC: the initial state (all initialize + integration guarantees) satisfies the START assertion.
//! Each proof fn has an empty body: Verus discharges `requires ==> ensures`
//! via SMT; add proof hints in the body if a VC does not discharge.

#![allow(non_snake_case)]
#![allow(unused_variables)]
#![allow(unused_imports)]

use vstd::prelude::*;

use crate::assertions::*;
use crate::tc_req_02::assertions::*;
use crate::contracts::*;
use crate::system_state::SystemState;
use crate::write_frames::*;

verus! {

/** VC[0]: Init-State -- all initialize + integration guarantees |- START */
pub proof fn vc_init_state(st: SystemState)
  requires
    sensor::initialize_initializes(st.sensedTemp),
    sensor::integration_spec_Sensor_Temperature_Range_guarantee(st.sensedTemp),
    control::initialize_defaultSetPoint(st.sv_currentSetPoint),
    control::initialize_defaultFanStates(st.sv_currentFanState),
    control::initialize_defaultLatestTemp(st.sv_latestTemp),
  ensures
    sys_assert_tc_req_02_START(st),
{}

} // verus!
