// Do not edit this file as it will be overwritten if HAMR codegen is rerun

//! C-interface for the component.
//! This code must be unsafe.
//! Assumptions about correctness are introduced and need to be verified by other means.

use data::*;

#[cfg(test)]
use std::sync::Mutex;

#[cfg(not(test))]
extern "C" {
  fn get_currentTemp(value: *mut TempControl_SysVerif::Temperature) -> bool;
  fn get_fanAck(value: *mut TempControl_SysVerif::FanAck) -> bool;
  fn get_setPoint(value: *mut TempControl_SysVerif::SetPoint) -> bool;
  fn put_fanCmd(value: *mut TempControl_SysVerif::FanCmd) -> bool;
}

pub fn unsafe_get_currentTemp() -> Option<TempControl_SysVerif::Temperature>
{
  unsafe {
    let value: *mut TempControl_SysVerif::Temperature = &mut TempControl_SysVerif::Temperature::default();
    if (get_currentTemp(value)) {
      return Some(*value);
    } else {
      return None;
    }
  }
}

pub fn unsafe_get_fanAck() -> Option<TempControl_SysVerif::FanAck>
{
  unsafe {
    let value: *mut TempControl_SysVerif::FanAck = &mut TempControl_SysVerif::FanAck::default();
    if (get_fanAck(value)) {
      return Some(*value);
    } else {
      return None;
    }
  }
}

pub fn unsafe_get_setPoint() -> Option<TempControl_SysVerif::SetPoint>
{
  unsafe {
    let value: *mut TempControl_SysVerif::SetPoint = &mut TempControl_SysVerif::SetPoint::default();
    if (get_setPoint(value)) {
      return Some(*value);
    } else {
      return None;
    }
  }
}

pub fn unsafe_put_fanCmd(value: &TempControl_SysVerif::FanCmd) -> bool
{
  unsafe {
    return put_fanCmd(value as *const TempControl_SysVerif::FanCmd as *mut TempControl_SysVerif::FanCmd);
  }
}

//////////////////////////////////////////////////////////////////////////////////
// Testing Versions
//////////////////////////////////////////////////////////////////////////////////

#[cfg(test)]
lazy_static::lazy_static! {
  // simulate the global C variables that point to the microkit shared memory regions.  In a full
  // microkit system we would be able to mutate the shared memory for out ports since they're r/w,
  // but we couldn't do that for in ports since they are read-only
  pub static ref IN_currentTemp: Mutex<Option<TempControl_SysVerif::Temperature>> = Mutex::new(None);
  pub static ref IN_fanAck: Mutex<Option<TempControl_SysVerif::FanAck>> = Mutex::new(None);
  pub static ref IN_setPoint: Mutex<Option<TempControl_SysVerif::SetPoint>> = Mutex::new(None);
  pub static ref OUT_fanCmd: Mutex<Option<TempControl_SysVerif::FanCmd>> = Mutex::new(None);
}

#[cfg(test)]
pub fn initialize_test_globals() {
  unsafe {
    *IN_currentTemp.lock().unwrap_or_else(|e| e.into_inner()) = None;
    *IN_fanAck.lock().unwrap_or_else(|e| e.into_inner()) = None;
    *IN_setPoint.lock().unwrap_or_else(|e| e.into_inner()) = None;
    *OUT_fanCmd.lock().unwrap_or_else(|e| e.into_inner()) = None;
  }
}

#[cfg(test)]
pub fn get_currentTemp(value: *mut TempControl_SysVerif::Temperature) -> bool
{
  unsafe {
    match *IN_currentTemp.lock().unwrap_or_else(|e| e.into_inner()) {
      Some(v) => {
        *value = v;
        return true;
      },
      None => return false,
    }
  }
}

#[cfg(test)]
pub fn get_fanAck(value: *mut TempControl_SysVerif::FanAck) -> bool
{
  unsafe {
    match *IN_fanAck.lock().unwrap_or_else(|e| e.into_inner()) {
      Some(v) => {
        *value = v;
        return true;
      },
      None => return false,
    }
  }
}

#[cfg(test)]
pub fn get_setPoint(value: *mut TempControl_SysVerif::SetPoint) -> bool
{
  unsafe {
    match *IN_setPoint.lock().unwrap_or_else(|e| e.into_inner()) {
      Some(v) => {
        *value = v;
        return true;
      },
      None => return false,
    }
  }
}

#[cfg(test)]
pub fn put_fanCmd(value: *mut TempControl_SysVerif::FanCmd) -> bool
{
  unsafe {
    *OUT_fanCmd.lock().unwrap_or_else(|e| e.into_inner()) = Some(*value);
    return true;
  }
}
