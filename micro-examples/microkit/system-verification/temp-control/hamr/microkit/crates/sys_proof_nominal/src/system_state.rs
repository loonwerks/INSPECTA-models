// Do not edit this file as it will be overwritten if HAMR codegen is rerun

//! Flat system state for the system-level verification conditions.
//! One field per AADL connection channel (owned by its source out port),
//! unconnected port, and GUMBO state variable across all components.

use data::*;
use vstd::prelude::*;

verus! {

pub struct SystemState {
  // -- TempControlSystem_Instance.tsp.tst --
  pub sensedTemp: Option<TempControl_SysVerif::Temperature>, // channel

  // -- TempControlSystem_Instance.tcp.tct --
  pub setPoint: Option<TempControl_SysVerif::SetPoint>, // channel
  pub fanCmd: Option<TempControl_SysVerif::FanCmd>, // channel
  pub sv_currentSetPoint: TempControl_SysVerif::SetPoint, // state variable
  pub sv_currentFanState: TempControl_SysVerif::FanCmd, // state variable
  pub sv_latestTemp: TempControl_SysVerif::Temperature, // state variable
  pub sv_fanError: bool, // state variable

  // -- TempControlSystem_Instance.fp.ft --
  pub fanAck: Option<TempControl_SysVerif::FanAck>, // channel
}

} // verus!
