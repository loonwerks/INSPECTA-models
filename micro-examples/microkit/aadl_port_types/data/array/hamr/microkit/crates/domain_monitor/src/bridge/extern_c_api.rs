// Do not edit this file as it will be overwritten if HAMR codegen is rerun

//! C-interface for the component.
//! This code must be unsafe.
//! Assumptions about correctness are introduced and need to be verified by other means.

use data::*;

#[cfg(test)]
use std::sync::Mutex;

#[cfg(not(test))]
extern "C" {
  fn get_producer_p_p_producer_write_port(value: *mut data_1_prod_2_cons_array::ArrayOfStruct) -> bool;
}

pub fn unsafe_get_producer_p_p_producer_write_port() -> data_1_prod_2_cons_array::ArrayOfStruct
{
  unsafe {
    let value: *mut data_1_prod_2_cons_array::ArrayOfStruct = &mut [data_1_prod_2_cons_array::struct_i::default(); data_1_prod_2_cons_array::data_1_prod_2_cons_array_ArrayOfStruct_DIM_0];
    get_producer_p_p_producer_write_port(value);
    return *value;
  }
}

//////////////////////////////////////////////////////////////////////////////////
// Testing Versions
//////////////////////////////////////////////////////////////////////////////////

#[cfg(test)]
lazy_static::lazy_static! {
  // simulate the global C variables that point to the microkit shared memory regions.  In a full
  // microkit system we would be able to mutate the shared memory for out ports since they're r/w,
  // but we couldn't do that for in ports since they are read-only
  pub static ref IN_producer_p_p_producer_write_port: Mutex<Option<data_1_prod_2_cons_array::ArrayOfStruct>> = Mutex::new(None);
}

#[cfg(test)]
pub fn initialize_test_globals() {
  unsafe {
    *IN_producer_p_p_producer_write_port.lock().unwrap_or_else(|e| e.into_inner()) = None;
  }
}

#[cfg(test)]
pub fn get_producer_p_p_producer_write_port(value: *mut data_1_prod_2_cons_array::ArrayOfStruct) -> bool
{
  unsafe {
    let guard = IN_producer_p_p_producer_write_port.lock().unwrap_or_else(|e| e.into_inner());
    *value = guard.expect("Not expecting None");
    true
  }
}
