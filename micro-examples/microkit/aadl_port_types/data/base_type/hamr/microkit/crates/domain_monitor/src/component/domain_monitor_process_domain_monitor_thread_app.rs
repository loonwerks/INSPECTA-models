// Do not edit this file as it will be overwritten if HAMR codegen is rerun

use data::*;
use crate::bridge::domain_monitor_process_domain_monitor_thread_api::*;
use vstd::prelude::*;

pub struct domain_monitor_process_domain_monitor_thread {
  // PLACEHOLDER MARKER STATE VARS
}

impl domain_monitor_process_domain_monitor_thread {
  pub fn new() -> Self
  {
    Self {
      // PLACEHOLDER MARKER STATE VAR INIT
    }
  }

  pub fn initialize<API: domain_monitor_process_domain_monitor_thread_Put_Api> (
    &mut self,
    api: &mut domain_monitor_process_domain_monitor_thread_Application_Api<API>)
  {
    log_info("initialize entrypoint invoked");
  }

  pub fn timeTriggered<API: domain_monitor_process_domain_monitor_thread_Full_Api> (
    &mut self,
    api: &mut domain_monitor_process_domain_monitor_thread_Application_Api<API>)
  {
    log_info("compute entrypoint invoked");
  }

  pub fn notify(
    &mut self,
    channel: microkit_channel)
  {
    // this method is called when the monitor does not handle the passed in channel
    match channel {
      _ => {
        log_warn_channel(channel)
      }
    }
  }
}

pub fn log_info(msg: &str)
{
  log::info!("{0}", msg);
}

pub fn log_warn_channel(channel: u32)
{
  log::warn!("Unexpected channel: {0}", channel);
}

// PLACEHOLDER MARKER GUMBO METHODS
