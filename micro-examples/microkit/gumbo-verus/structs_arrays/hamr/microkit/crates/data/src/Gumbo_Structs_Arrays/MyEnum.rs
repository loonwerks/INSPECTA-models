// Do not edit this file as it will be overwritten if HAMR codegen is rerun

use vstd::prelude::*;

use super::*;

verus! {

  #[repr(C)]
  #[derive(Copy, Clone, Debug, PartialEq, Eq)]
  pub enum MyEnum {
    pub On = 0,
    pub Off = 1,
  }

  impl Default for MyEnum {
    fn default() -> Self
    {
      MyEnum::On
    }
  }

}

// Memory layout as HAMR computes it
const _: () = assert!(core::mem::size_of::<MyEnum>() == 4);
const _: () = assert!(core::mem::align_of::<MyEnum>() == 4);
