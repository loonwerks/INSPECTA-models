// Do not edit this file as it will be overwritten if HAMR codegen is rerun

//! C-interface for the component.
//! This code must be unsafe.
//! Assumptions about correctness are introduced and need to be verified by other means.

use data::*;

#[cfg(test)]
use std::sync::Mutex;

#[cfg(not(test))]
extern "C" {
  fn get_producer_producer_myStructArray(value: *mut Gumbo_Structs_Arrays::MyStructArray_i) -> bool;
  fn get_producer_producer_MyArrayStruct(value: *mut Gumbo_Structs_Arrays::MyArrayStruct) -> bool;
}

pub fn unsafe_get_producer_producer_myStructArray() -> Option<Gumbo_Structs_Arrays::MyStructArray_i>
{
  unsafe {
    let value: *mut Gumbo_Structs_Arrays::MyStructArray_i = &mut Gumbo_Structs_Arrays::MyStructArray_i::default();
    if (get_producer_producer_myStructArray(value)) {
      return Some(*value);
    } else {
      return None;
    }
  }
}

pub fn unsafe_get_producer_producer_MyArrayStruct() -> Option<Gumbo_Structs_Arrays::MyArrayStruct>
{
  unsafe {
    let value: *mut Gumbo_Structs_Arrays::MyArrayStruct = &mut [Gumbo_Structs_Arrays::MyStruct2_i::default(); Gumbo_Structs_Arrays::Gumbo_Structs_Arrays_MyArrayStruct_DIM_0];
    if (get_producer_producer_MyArrayStruct(value)) {
      return Some(*value);
    } else {
      return None;
    }
  }
}

//////////////////////////////////////////////////////////////////////////////////
// Testing Versions
//////////////////////////////////////////////////////////////////////////////////

#[cfg(test)]
lazy_static::lazy_static! {
  // simulate the global C variables that point to the microkit shared memory regions.  In a full
  // microkit system we would be able to mutate the shared memory for out ports since they're r/w,
  // but we couldn't do that for in ports since they are read-only
  pub static ref IN_producer_producer_myStructArray: Mutex<Option<Gumbo_Structs_Arrays::MyStructArray_i>> = Mutex::new(None);
  pub static ref IN_producer_producer_MyArrayStruct: Mutex<Option<Gumbo_Structs_Arrays::MyArrayStruct>> = Mutex::new(None);
}

#[cfg(test)]
pub fn initialize_test_globals() {
  unsafe {
    *IN_producer_producer_myStructArray.lock().unwrap_or_else(|e| e.into_inner()) = None;
    *IN_producer_producer_MyArrayStruct.lock().unwrap_or_else(|e| e.into_inner()) = None;
  }
}

#[cfg(test)]
pub fn get_producer_producer_myStructArray(value: *mut Gumbo_Structs_Arrays::MyStructArray_i) -> bool
{
  unsafe {
    match *IN_producer_producer_myStructArray.lock().unwrap_or_else(|e| e.into_inner()) {
      Some(v) => {
        *value = v;
        return true;
      },
      None => return false,
    }
  }
}

#[cfg(test)]
pub fn get_producer_producer_MyArrayStruct(value: *mut Gumbo_Structs_Arrays::MyArrayStruct) -> bool
{
  unsafe {
    match *IN_producer_producer_MyArrayStruct.lock().unwrap_or_else(|e| e.into_inner()) {
      Some(v) => {
        *value = v;
        return true;
      },
      None => return false,
    }
  }
}
