// Do not edit this file as it will be overwritten if HAMR codegen is rerun

//! Property system_output_range -- Init-State VC: the initial state (all initialize + integration guarantees) satisfies the START assertion.
//! Each proof fn has an empty body: Verus discharges `requires ==> ensures`
//! via SMT; add proof hints in the body if a VC does not discharge.

#![allow(non_snake_case)]
#![allow(unused_variables)]
#![allow(unused_imports)]

use vstd::prelude::*;

use crate::assertions::*;
use crate::system_output_range::assertions::*;
use crate::contracts::*;
use crate::system_state::SystemState;
use crate::write_frames::*;

verus! {

/** VC[0]: Init-State -- all initialize + integration guarantees |- START */
pub proof fn vc_init_state(st: SystemState)
  requires
    outer_a::initialize_A_Initial_Input_Range(st.top_output),
    a::initialize_A_Initial_Input_Range(st.system_input),
  ensures
    sys_assert_system_output_range_START(st),
{}

} // verus!
